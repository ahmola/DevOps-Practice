spring:
    application:
        name: crud-get
    config:
        import: "configserver:http://config-server:8888"
    cloud:
        config:
            fail-fast: true
            retry:
                max-attempts: 10
                initial-interval: 2000